<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/actionbar</item>
        <item name="colorPrimaryDark">@color/actionbarDunkler</item>
        <item name="colorAccent">@color/actionbar</item>
        <item name="tabBackground">@color/actionbar</item>
        <item name="tabIndicatorColor">@color/actionbarDunkler</item>
    </style>

    <style name="TabLayoutTheme" parent="Widget.Design.TabLayout">
        <item name="tabBackground">@color/actionbar</item>
        <item name="tabIndicatorColor">@color/actionbarDunkler</item>
    </style>
</resources>
